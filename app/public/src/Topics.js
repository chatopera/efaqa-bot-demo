var Topics = {
  USER_CONNECTED: "user-connected",
  USER_DISCONNECTED: "user-disconnected",
  USER_MESSAGE: "user-message",
  SUPERSCRIPT_RESPONSE: "server-response"
};

if (typeof module !== "undefined") {
  module.exports = Topics;
}
